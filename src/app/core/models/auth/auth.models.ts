export interface AuthResponse {
  id: string;
  name: string;
  lastName: string;
  username: string;
  email: string;
  token: string;
  foundationId: string;
}

export interface User {
  id: string;
  name: string;
  lastName: string;
  username: string;
  email: string;
  foundationId: string;
}

